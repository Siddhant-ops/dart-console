import 'package:console_project_one/console_project_one.dart'
    as console_project_one;

void main(List<String> arguments) {
  // try {
  //   int args = arguments.fold(
  //       0, (previousValue, element) => previousValue + int.parse(element));
  //   print(
  //       'Hello world: ${console_project_one.calculate()}!\nargs total is $args');
  // } catch (e) {
  //   print("Error: $e");
  // }
  print('Hello, World!');
}
